on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - run: rsync -a /home/mara/actions-runner/_work/Crunchy-Patrol/ /home/mara/Bot/
